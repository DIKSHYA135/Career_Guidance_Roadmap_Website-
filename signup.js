// Signup JS
document.addEventListener("DOMContentLoaded", () => {
    const form = document.querySelector('form');
    form.addEventListener('submit', (e) => {
        // e.preventDefault();
        // Built-in HTML validation minlength="6" handles the password length check.
    });
});
